# JAX/MJX强化学习训练框架依赖
# 安装方法: pip install -r requirements.txt

# JAX核心（CPU优先，先保证可用，后续再启用GPU插件）
jax==0.8.1
jaxlib==0.8.1

# 可选：JAX CUDA 插件（GPU 后端）
jax-cuda12-plugin==0.8.1
jax-cuda12-pjrt==0.8.1

# MuJoCo物理引擎
mujoco>=3.0.0
mujoco-mjx  # MJX (MuJoCo JAX backend)

# NumPy版本限制（与JAX 0.8.1兼容）
numpy>=2.0.0,<2.4.0

# 神经网络和优化
flax>=0.8.0
optax>=0.1.7

# 进度条和终端美化
rich>=13.0.0

# 检查点和日志
orbax-checkpoint>=0.4.0
tensorboardX>=2.6.0

# 工具库
numpy>=1.24.0,<2.0.0
pyyaml>=6.0
matplotlib>=3.7.0
mediapy>=1.1.0

# 开发工具
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0
